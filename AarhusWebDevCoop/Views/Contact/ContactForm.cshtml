@model AarhusWebDevCoop.Models.ContactModel
@{
    Layout = null;
}

@if (TempData["success"] == null)
{
    using (Html.BeginUmbracoForm("ContactForm", "Contact", new object { }))
    {
        <div class="form-group">
            @Html.LabelFor(m => m.Name, new { @class = "textHeadline" })
            @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Name)
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Subject, new { @class = "textHeadline" })
            @Html.TextBoxFor(m => m.Subject, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Subject)
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Email, new { @class = "textHeadline" })
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Email)
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Message, new { @class = "textHeadline" })
            @Html.TextAreaFor(m => m.Message, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Message)
        </div>
        <div class="form-group">
            <input type="submit" class="btnColor btn-success" value="Send"/>
        </div>
    }
}
else
{
    <div class="alert alert-success"><span class="glyphicon glyphicon-ok"></span> Your message is sent.</div>
}